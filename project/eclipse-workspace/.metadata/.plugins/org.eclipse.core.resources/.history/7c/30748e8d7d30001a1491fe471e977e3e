package org.io.jdbc.ignite.test;

import static com.google.common.base.Preconditions.checkState;

import java.sql.Connection;
import java.sql.DatabaseMetaData;
import java.sql.DriverManager;
import java.sql.ResultSet;

import org.apache.pulsar.io.jdbc.ignite.JdbcUtils.TableId;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
        System.out.println( "Hello World!" );
    	try{
    		// Register JDBC driver.
    		Class.forName("org.apache.ignite.IgniteJdbcThinDriver");
    		 
    		// Open the JDBC connection.
    		Connection connection = DriverManager.getConnection("jdbc:ignite:thin://104.211.24.238");
    		 DatabaseMetaData metadata = connection.getMetaData();
    	        try (ResultSet rs = metadata.getTables(null, null, "\"9E7B08B5-3BFD-4026-B322-C4A34FFFAFD7\".ENTITIESTABLEINFO", new String[]{"TABLE"})) {
    	        	System.out.println(rs);
    	            if (rs.next()) {
    	                String catalogName = rs.getString(1);
    	                String schemaName = rs.getString(2);
    	                String gotTableName = rs.getString(3);
    	              
    	            } else {
    	                throw new Exception("Not able to find table: ");
    	            }
    	        }
    		}
    		catch(Exception ex){
    			System.out.println("---"+ex);
    		}
    }
}
